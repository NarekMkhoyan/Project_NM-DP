import { NmTriggerDirective } from './nm-trigger.directive';

describe('NmTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new NmTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
