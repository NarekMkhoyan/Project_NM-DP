import { NmCancelActionDirective } from './nm-cancel-action.directive';

describe('NmCancelActionDirective', () => {
  it('should create an instance', () => {
    const directive = new NmCancelActionDirective();
    expect(directive).toBeTruthy();
  });
});
