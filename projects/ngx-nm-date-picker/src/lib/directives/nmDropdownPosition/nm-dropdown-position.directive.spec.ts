import { NmDropdownPositionDirective } from './nm-dropdown-position.directive';

describe('NmDropdownPositionDirective', () => {
  it('should create an instance', () => {
    const directive = new NmDropdownPositionDirective();
    expect(directive).toBeTruthy();
  });
});
