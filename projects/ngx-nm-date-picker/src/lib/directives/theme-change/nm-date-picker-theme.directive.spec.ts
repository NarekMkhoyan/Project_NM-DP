import { NmDatePickerThemeDirective } from './nm-date-picker-theme.directive';

describe('NmDatePickerThemeDirective', () => {
  it('should create an instance', () => {
    const directive = new NmDatePickerThemeDirective();
    expect(directive).toBeTruthy();
  });
});
