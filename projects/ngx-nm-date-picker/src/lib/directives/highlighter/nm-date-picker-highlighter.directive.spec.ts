import { NmDatePickerHighlighterDirective } from './nm-date-picker-highlighter.directive';

describe('NmDatePickerHighlighterDirective', () => {
  it('should create an instance', () => {
    const directive = new NmDatePickerHighlighterDirective();
    expect(directive).toBeTruthy();
  });
});
