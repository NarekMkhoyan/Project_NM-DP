import { NmDatePickerSelectorStateDirective } from "./nm-date-picker-selector-state.directive";

describe('NmDatePickerSelectorStateDirective', () => {
  it('should create an instance', () => {
    const directive = new NmDatePickerSelectorStateDirective();
    expect(directive).toBeTruthy();
  });
});
