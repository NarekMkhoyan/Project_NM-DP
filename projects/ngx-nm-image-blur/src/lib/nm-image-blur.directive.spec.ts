import { NmImageBlurDirective } from './nm-image-blur.directive';

describe('NmImageBlurDirective', () => {
  it('should create an instance', () => {
    const directive = new NmImageBlurDirective();
    expect(directive).toBeTruthy();
  });
});
